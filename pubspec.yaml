name: ai_video_analyzer
description: AI视频实时抓取分析APP
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.2
  camera: ^0.10.5+5
  path_provider: ^2.1.1
  image_picker: ^1.0.4
  provider: ^6.1.1
  webview_flutter: ^4.4.0
  fluttertoast: ^8.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true